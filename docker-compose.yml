version: "3.9"

services:
  job-monitor:
    build: .
    container_name: job-monitor
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
